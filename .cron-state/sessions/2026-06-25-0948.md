# Batch 31 — 2026-06-25 09:48 PT

Tick context: lock acquired (TICK_PROCEED), branch main, working directly on
main per the authoritative prompt. The injected task note claimed 31 unpushed
commits + missing node_modules/.cron-state, but the actual repo state was
`main == origin/main` (0 ahead), node_modules present, .cron-state present —
this is tick 31 of an established loop, not a bootstrap. Followed the
authoritative prompt (commit straight to main, no feature branch).

Frontend-focus override (2026-06-23) still in effect — all 5 slices are
FRONTEND/UX.

## Shipped (5/5) — all pushed 22b5262..de2a8e1

1. R31.41 — clear-history affordance on the bias import scope-history pip
   (76a674d). New pure clearImportScopeHistory(storage); RightSidebar re-seeds
   the head from the live scope after the wipe; tabIndex=-1 x button inside the
   aria-hidden dots wrapper. ~8 asserts.

2. R31.42 — pulse the bulk-unpin depth pip on increment (731794f). pulse token
   bumped from setBulkUnpinChain (event-time) only when depth grows to >=2; pip
   keyed {depth}-{pulse} to remount + replay a scale/indigo-bloom keyframe;
   reduced-motion gated. Pure UI.

3. R31.43 — interactive fade-preview swatch replays on click (f83e407). Swatch
   becomes its own sibling button (no button-in-button); playToken re-keys the
   anim effect — loop restart (normal) or single self-clearing pass (RM, the
   only way a reduced-motion user can feel the curve).

4. R31.45 — tri-state none/some/all clamp Select-all toggle (3af5523). New pure
   clampSelectAllTriState(sel, tot) in midiMap.js; toggle gains an amber
   indeterminate dash box for 'some' distinct from empty 'none' + indigo 'all'.
   ~17 asserts.

5. R31.20 — keyboard reorder + aria-live narration for the attractor list
   (de2a8e1). #N badge is now a tabIndex=0 role=button grab handle
   (lift/arrow/commit/Escape); describeGapReorderAnnouncement phrases each step
   into a visually-hidden aria-live region; lifted badge pulses indigo + the
   keyboard cursor lights the same gap strips the mouse uses. Pure wire layer on
   already-pinned helpers. Stale lift gated at render via derived liftActive
   flag (NO setState-in-effect — lint stays at baseline).

## Gates (run ONCE for the batch)

- Lint: `npm run lint` → 23 errors / 3 warnings = EXACTLY baseline. All
  pre-existing in ParticleCanvas.jsx + vite.config.js. The only LeftSidebar
  error is the pre-existing setDraftName-in-effect sync; R31.20 added ZERO new
  lint errors (deliberately avoided a second setState-in-effect by using a
  derived liftActive flag instead of a clear-on-list-change effect).
- Build: `npm run build` → green, ~748ms, 2422 modules transformed.
- Tests: ALL 40 test files pass. ~25 fresh asserts this batch
  (biasOverridesIO +8, midiMap +17; R31.20/R31.42/R31.43 reuse already-pinned
  pure helpers + are UI-only).

## Notes / decisions

- Two patch calls timed out mid-stream on large edits; broke them into smaller
  patches. No content lost.
- R31.43 required restructuring the cycle chip: the swatch used to render as a
  <span> INSIDE the cycle <button>. Making it independently clickable meant
  moving it OUT to a sibling button (button-in-button is invalid). Cycle chip
  keeps glyph + label; swatch is now a separate replay button next to it.
- R31.45 test had one wrong assertion (expected Infinity->all); corrected to
  Infinity->none since Number.isFinite(Infinity) is false and the helper
  coerces non-finite to 0 (intended defensive behavior). Fixed + green.

## Next (Batch 32 — refilled in STATE.md)

R32.41 (undo toast after scope-history clear), R32.42 (pulse pip on DECREMENT
too), R32.43 (long-press swatch to pin the loop), R32.45 (three-way cycle /
invert from the 'some' state), R32.20 (visible "lifted" floating chip for
sighted keyboard users).
