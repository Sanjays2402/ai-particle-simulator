# Tick session 2026-06-23 10:46 PT — Batch 25 (5/5)

Cake (cron). Lock acquired 10:46:24 PT, released after batch landed on
origin/main.

## Features shipped (5/5 — all graduations of Batch 24 slices)

1. **R25.41** — `feat(bias): import preview shows per-FIELD diff inline per chip row` (18024b6)
   - New pure projectors `buildBiasFieldDiff(incoming, live)` + `biasFieldValuesEqual(field, a, b)` in `biasOverridesIO.js`.
   - Each chip row in the import preview becomes expandable; expanded panel shows ADD/CHG/DROP badges per field, kind chip (range / chance / list), and inline before→after value summary using a compact number formatter.
   - Field ordering is canonical (4 ranges → 10 chances → forceTypes).
   - Merge mode hides 'live-only' rows (preserves untouched); replace mode shows them.
   - ~50 fresh asserts.

2. **R25.42** — `feat(carousel): note filter regex pattern history dropdown` (f995349)
   - Recent-patterns dropdown so the power user re-typing "^demo$" / "TODO|FIXME" / "good demo shot" picks a saved pattern in one click.
   - New lib in `presetThumbnails.js`: `NOTE_FILTER_HISTORY_MAX=12`, `NOTE_FILTER_HISTORY_QUERY_MAX=256` (ReDoS guard), `sanitizeNoteFilterHistory` / `loadNoteFilterHistory` / `saveNoteFilterHistory` / `addNoteFilterHistoryEntry` (MRU semantics, ref-equal-on-no-op) / `removeNoteFilterHistoryEntry` / `clearNoteFilterHistory`.
   - Storage round-trip through `preset-note-filter-history-v1`.
   - UI: cyan stacked-tile icon button next to `.*` mode toggle; anchored absolute dropdown with backdrop click-outside.
   - Enter / X-close commit; Down-arrow surfaces dropdown for keyboard nav; double-Escape (close dropdown → clear + close input).
   - ~70 fresh asserts.

3. **R25.43** — `feat(midi): hotkey UNDO chain badge alternates direction glyph` (6da5357)
   - Badge text now carries an alternating direction glyph: `«x2` (first restore) → `»x3` (re-undo back) → `«x4` etc.
   - New pure helper `directionGlyphForChainStep(chainStep)` in `midiPresets.js`.
   - `formatHotkeyChainBadge` extended: embeds glyph as text prefix + exposes `direction` field for UI callers; tooltip mentions the glyph + step number.
   - R24.38 existing tests updated to new badge format.
   - ~25 fresh asserts (incl. 19-step full-sweep regression that every consecutive pair must flip).

4. **R25.45** — `feat(midi): clamp popover bulk-clear all per-field overrides for attractor` (8159d65)
   - "Clear all (N)" button in `ClampThresholdPopover` footer (violet, matches per-attractor scope).
   - Only renders when the attractor has 2+ per-field overrides (1 override = same as "Clear attr." for current field — redundant).
   - Count badge surfaces exact wipe scope.
   - New pure projector `countClampWarnAttractorOverridesFor(overrides, attractorId)` in `midiMap.js`.
   - Re-uses existing `clearClampWarnAttractorOverrides` for the actual wipe.
   - ~25 fresh asserts.

5. **R25.20** — `feat(midi): per-attractor binding-group drag-and-drop reorder` (c08cedc)
   - Each binding group header gets a braille-pattern (`⠇`) grab handle when ≥2 entries.
   - Stale groups (deleted attractor still has bindings) are NOT draggable.
   - Live position "#N" badge next to type chip keeps user oriented.
   - Violet drop-target highlight; dragged group dims to 0.55 opacity.
   - Pure wire-layer on existing primitives (`moveAttractorByIndex` from R15.20 tests, sibling-enter-before-leave guard from R17.07 / R18.19).
   - New store action `moveNamedAttractorByIndex(fromIdx, toIdx)` wraps lib helper with standard ref-equal-on-no-op + save-then-set pattern.
   - Order of headers in panel ↔ order of attractors in scene, both directions.

## Quality gates (run once at end of batch)

- **Lint:** 26 problems (23 errors + 3 warnings) — exactly matches baseline. Zero new errors in any of the 7 modified .js/.jsx files + 4 modified .test.mjs files. No lint-gate cleanup commits needed.
- **Build:** 1.61 s green. 1.84 MB bundle, gzip 541 KB (+0 KB vs Batch 24 — most slices are pure wire layers).
- **Unit tests:** 40/40 files pass. ~190 fresh asserts this batch.

## Commits pushed

```
c08cedc feat(midi): per-attractor binding-group drag-and-drop reorder
8159d65 feat(midi): clamp popover bulk-clear all per-field overrides for attractor
6da5357 feat(midi): hotkey UNDO chain badge alternates direction glyph
f995349 feat(carousel): note filter regex pattern history dropdown
18024b6 feat(bias): import preview shows per-FIELD diff inline per chip row
```

`origin/main` verified after push (HEAD = c08cedc).

## Notes for next tick

- Batch 25 substituted R25.20, R25.41-R25.43, R25.45 from the future queue — every slice is a direct graduation of a Batch 24 feature, giving the batch a tight thematic spine (Batch 24 → Batch 25 iteration loop without padding).
- R25.01-R25.05 (Echo / trail FBO, 3D drag handle, server-rendered OG endpoint, multi-error gutter overlay, custom audio-reactive curve editor) deferred yet again — each needs its own focused / infrastructure-heavy batch.
- R25.44 (camera path touch DnD haptic feedback) NOT shipped this tick — would be a small slice but the navigator.vibrate path is platform-specific (Android only); deferred to a batch that bundles other haptic / platform-conditional polish.
- Roadmap refilled with R26.41-R26.45 + R26.20 (graduations of THIS batch's features) + carried R25.06-R25.26 from the existing queue.
- Build chunk-size warning still pending; documented but not yet addressed.
