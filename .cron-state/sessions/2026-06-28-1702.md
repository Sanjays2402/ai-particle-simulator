# Tick 47 — Batch 47 (5/5) — 2026-06-28 17:02 PT

Frontend-focus override active. Five frontend slices, all graduations of
Batch 46, each its own commit. Gate ONCE at end: lint 23/3 == baseline
(zero new), build green, 48 test files pass.

## Shipped
- R47.M 61a1993 — framingGuides.pinnedDropCaretGap + LeftSidebar 2px violet
  insert caret between pinned ratio chips at the drop gap (Fragment-wrapped
  chips, trailing caret when gap===count). +13 asserts.
- R47.E 79928b7 — zenMode.formatCustomFramingLine + ZenMode: custom crop
  reads "Custom · 2.35" not bare ratio. +10 asserts.
- R47.G 7ecd0c6 — screenshotWatermark.previewGhostPosition + TopBar ghost
  pill following pointer mid caption-drag (clamped, hide on up/cancel). +12.
- R47.H 208c8d8 — cameraViews.rangePreviewSet + CommandPalette: anchor..hover
  block lights up (fill + left stripe) before the completing click. +11.
- R47.N 48b93e3 — minimap.fitDotGlyphs + Minimap: Fit N button dot cluster
  (lit/dim) matching dimmed dots, cap 6 + overflow. +18.

## Notes
- Trigger boilerplate ("31 unpushed / first tick / npm install / branch off
  LOCAL HEAD") STALE again — repo in sync at d0d623a, node_modules present,
  STATE bootstrapped. Worked directly on main, pushed to origin.
- Roadmap: Batch 47 done; queued Batch 48 (graduations of 47) + carried items.
