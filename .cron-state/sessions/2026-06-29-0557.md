# Batch 52 — 2026-06-29 05:57 PT

Tick 52. Frontend-focus override active. Target 5, shipped 5 genuinely-new
user-facing features. Worked DIRECTLY on main (per authoritative prompt),
gated once at the end, pushed clean.

## Shipped (5 commits, each its own lib + component pair)

1. **R52.N — Minimap numbered saved-view badges** (`b0c03cc`)
   - minimap.js: `projectSavedViews` stamps a 1-based `order` on each VALID
     marker (skipped malformed rows don't punch gaps). New pure
     `savedViewMarkerLabel(order, cap=99)` (>cap → "99+", sub-1/non-finite → '').
   - Minimap.jsx: draws the digit up-and-right of each dot, dark backing
     stroke for legibility, inherits dot dim alpha.
   - +17 asserts.

2. **R52.O — HUD linked/diverged glyph** (`3febdaa`)
   - debugHud.js: pure `hudViewsLinked(modes)` (fps==ms after sanitize).
   - DebugHUD.jsx: violet ≡ when synced, dim ≠ when diverged, mode-aware tooltip.
   - +9 asserts.

3. **R52.H — type-DELETE friction gate for 30+ prune** (`3ea71f9`)
   - cameraViews.js: RANGE_TYPE_CONFIRM_AT=30 (above huge tier 20);
     `rangeNeedsTypeConfirm`, `matchesRangeConfirmPhrase` (case-insensitive +
     trimmed), `rangeTypeConfirmPrompt`.
   - CommandPalette.jsx: deleteSelected routes 30+ through window.prompt;
     cancel/non-match aborts; <30 keeps one-click confirm.
   - +22 asserts.

4. **R52.G — watermark caption size chips S/M/L** (`56d7199`)
   - screenshotWatermark.js: WATERMARK_SIZES roster + sanitize/cycle/mult +
     `watermarkFontSizeForPreset` (medium == prior default exactly; clamp band kept).
   - store.js: screenshotWatermarkSize (own key). TopBar.jsx: chip row +
     bake path + live preview honour the size; dropped unused watermarkFontSize import.
   - +25 asserts.

5. **R52.B — easing-preview pace label + hover-gated dot** (`4753df3`)
   - framingGuides.js: EASING_PREVIEW_LOOP_SEC const + pure
     `easingPreviewPaceLabel(sec)` ("1.6s . 0.6Hz", trims ".0", blank on junk).
   - LeftSidebar.jsx: pace label in the Sweep-easing header (brightens while
     playing); dot only animates while the chip row is hovered, else rests at curve end.
   - +12 asserts.

## Gate (once, end of batch)
- lint: `npm run lint` → 26 problems (23 err / 3 warn) == baseline, ZERO new.
  (Verified each component's pre-existing errors via git-stash A/B comparison.)
- build: `npm run build` → green, 805ms.
- tests: `node --test src/lib/*.test.mjs` → 49 files pass, 0 fail. +85 asserts total.

## Push
- origin b591d1f..4753df3 (5 commits). Verified: origin/main top = 4753df3, 0 ahead / 0 behind.

## Notes / flags
- Trigger boilerplate ("31 unpushed local commits / first tick / npm install /
  branch feature/autoship off LOCAL HEAD") was STALE and CONTRADICTED the
  authoritative prompt: repo was already in sync at b591d1f (0 ahead of origin),
  node_modules present, and the prompt mandates working DIRECTLY on main
  (feature-branch commits don't show on the contribution graph). Followed the
  prompt. Same staleness was flagged in batches 50 + 51.
- Sibling cron agents were active in this repo during the tick (shared /tmp +
  test-file touch warnings). Mitigation: staged each commit's files EXPLICITLY
  by path (never `git add -A`), used per-slice commit-message tmp files. All 5
  commits verified clean (only my intended files in each).
- 4 Batch-52 items remain (E, M, K, A) — refilled roadmap with Batch 53
  (15 items, graduations of this batch + new) so the queue stays >5 deep.
