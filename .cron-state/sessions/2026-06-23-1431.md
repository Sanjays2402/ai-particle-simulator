# Batch 26 session — 2026-06-23 14:31 PT

5/5 shipped. Tight thematic spine: every slice graduates a Batch 25
feature — the "iteration loop" pattern from Batch 23 → 25 continues.

## Commits

- `bf8d333` — R26.41 bias preview chip-row changed/unchanged field counts
- `f13fbd2` — R26.42 note filter history entries can be PINNED to top
- `d43b4c2` — R26.43 hotkey UNDO chain badge colour-codes by direction
- `a898502` — R26.45 clamp popover "Clear all for THIS FIELD across attractors"
- `f25f073` — R26.20 MidiPanel binding-group DnD touch / long-press support

## Slice summary

### R26.41 (graduates R25.41)
Inline "Nc·Nu" pip on every chip row in BiasOverridesImportPreview so
the user can see how many fields actually MATTER without first expanding
the row. Lib: `summarizeFieldDiffCounts(fieldDiff, mode)` with mode-aware
live-only semantics (merge: not-counted, replace: counted as 'changed').

### R26.42 (graduates R25.42)
Right-click any history row OR click its star to pin; pinned entries
sort to the top of the dropdown (preserving MRU within tier) and survive
the cap drop. Lib: `togglePinNoteFilterHistoryEntry` +
`sortNoteFilterHistoryForDisplay`. Updated contracts: sanitize keeps
pins past cap; add preserves pinned flag on bump; cap drops oldest
UNPINNED.

### R26.43 (graduates R25.43)
Cyan badge for « (reverse), amber for » (forward). Lib:
`directionColorForChainStep` + HOTKEY_CHAIN_COLOR_REVERSE/FORWARD.
`formatHotkeyChainBadge` extended with color field; UI drops
winner-accent override so directional colour drives badge.

### R26.45 (graduates R25.45)
Transposed axis: instead of "wipe every field for this attractor",
this button wipes "this field's per-attractor override on every
attractor". Lib: `countClampWarnFieldOverridesAcross` +
`clearClampWarnFieldOverridesAcross` with last-cell-prune +
ref-equal-on-no-op + corrupt-inner-preserved contract.

### R26.20 (graduates R25.20)
350ms long-press touch DnD for binding-group reorder. Pure wire layer
on resolveTouchTargetIdx (R22.12) + moveAttractorByIndex (R15.20) +
sibling-enter-before-leave guard (R17.07 / R18.19). No new lib helpers
or tests — primitives all already pinned.

## Gates

- Lint: 23 errors / 3 warnings (matches baseline exactly; zero new)
- Build: 6.13 s green, 1.84 MB / gzip 542 KB (+1 KB vs Batch 25)
- Tests: 40/40 files pass (~155 fresh asserts this batch)

## Bumps

- biasOverridesIO: +1 helper (`summarizeFieldDiffCounts`)
- presetThumbnails: +2 helpers (pin toggle + sort projector)
- midiPresets: +2 colour constants + 1 helper (`directionColorForChainStep`),
  formatHotkeyChainBadge now surfaces color field
- midiMap: +2 helpers (count + clear per-field-across-attractors)
- midiPanel: lifts `resolveTouchTargetIdx` from cameraViews lib for touch
  DnD wire layer (no duplication of the half-open-interval math)

## Roadmap status

Batch 26 done. Batch 27 queue pre-filled with graduations of THIS batch's
slices (TOTAL-fields-touched count for R26.41; bulk-unpin for R26.42;
fade-over-window for R26.43; preview chips for R26.45; gap-drop zones
for R26.20).

Infrastructure batches (R26.01-R26.05 / R27.01-R27.05) still deferred —
same render-pipeline / r3f-gizmo / backend-runtime that need their own
dedicated batches. Nothing surfaced this tick to change that calculus.
