# Tick 38 — 2026-06-26 21:28 PT

Batch 38 (5/5). Frontend-focus override active. Worked DIRECTLY ON main.

## Stale trigger-message note (recurring)
The trigger said "31 unpushed commits / first tick / branch feature/autoship /
npm install since node_modules wasn't moved." ALL stale boilerplate again:
- main == origin/main at 98f4573, **0 commits ahead** — zero unpushed work to
  preserve. The "branch feature/autoship off LOCAL HEAD to preserve 31 commits"
  instruction was moot (a `feature/autoship` branch DOES exist on origin, 179
  commits behind main — an abandoned early-tick artifact; left untouched).
- node_modules present (185 pkgs) — no npm install needed.
- STATE.md bootstrapped through tick 37 — not the first tick.
Followed the authoritative prompt file: ship on main (feature-branch commits
don't show on the contribution graph), gate once at the end, no PR/tag/release.

## Shipped (5 commits, one slice each)
- **a0567af R38.A** — Debug HUD ETA-to-budget-edge readout. headroomEtaToEdge
  least-squares-fits the headroom slope over the live 2s window and extrapolates
  to "~Ns until the 16.7ms budget edge", shown only while falling AND above the
  edge. lib/fpsGraph.js (+ ETA_MAX_SEC, linregSlope). +39 asserts. "To edge" row
  in the ms-view trend section, red at <=3s.
- **36463be R38.B** — Golden-spiral one-shot draw-on sweep. stroke-dash reveal
  (dasharray+dashoffset = polylineLength, animate offset->0) + eye fade-in; keyed
  on spiralOrientation so it remounts/replays on corner change; reduced-motion →
  spiralLen 0 = no anim. lib/framingGuides.js polylineLength (+17 asserts) +
  spiral-sweep/spiral-eye-in keyframes. Derived at render (no state/effect/ref) to
  stay lint-clean.
- **3a3ad01 R38.D** — Perf-pill popover band-coloured fps sparkline. The same 12s
  window split into green/amber/red runs so a dip paints red where it happened.
  lib/fpsGraph.js buildBandedSparklineSegments (per-segment dest-band colour,
  shared boundaries) (+22 asserts). PerfBudgetPill popover-top SVG.
- **7f0ac8f R38.H** — Palette rename-saved-view inline + clear-all. Completes the
  saved-view lifecycle (save/restore/delete already there). renameView (ref-equal
  no-op on blank/identical/missing) + buildCameraRenameActions (~30 asserts).
  Inline input (Enter/Esc) + Clear All Saved Views action.
- **2f8f3e5 R38.K** — Per-motion calm gating. Long-press/right-click the calm
  button → popover to pick WHICH of 4 motions calm pauses. lib/calmMode.js
  resolveCalmFor / sanitizeCalmGates / toggleCalmGate / countGatedMotions /
  defaultCalmGates / CALM_MOTION_IDS (+71 asserts; back-compat invariant pinned:
  default gates ⇒ resolveCalmFor === resolveCalm). Rewired all 4 consumers
  (ParticleCanvas hue/shake/auto-rotate, ZenMode orbit) + store calmGates slice.

## Gates (ran ONCE at end of batch)
- Lint: EXACTLY baseline — 26 problems / 23 errors / 3 warnings, every one
  pre-existing (DebugHUD performance.now, ParticleCanvas unused-vars+impure,
  Toast, TopBar glow+escapes, store unused-e, SnapshotGallery, Timeline,
  vite.config). Targeted eslint proved every NEW lib + all touched components
  lint 100% clean (only line-shifted pre-existing errors in touched files).
  NOTE: hit a react-hooks/refs + react-hooks/set-state-in-effect wall on the
  first R38.B attempt (ref-mutation + setState during render) — resolved by
  deriving the sweep purely from the polyline `key={spiralOrientation}` (no
  state/effect/ref at all), which is both simpler and lint-clean.
- Build: green (~830ms, vite). dist/ gitignored — no artifacts committed.
- Tests: ALL 46 lib test files pass. ~179 fresh asserts (fpsGraph +61,
  framingGuides +17, cameraViews +30, calmMode +71).

## Push
98f4573..2f8f3e5 -> origin/main (+ the STATE/session chore commit). Verified
fast-forward after push.

## Next tick (Batch 39 refilled in STATE.md)
R39.A ETA history strip · R39.B spiral replay button · R39.D popover hover-scrub
· R39.H duplicate saved view · R39.K calm-gate map export/import · plus carried
R3x.C/E/G/L + R38.M/N/O.
