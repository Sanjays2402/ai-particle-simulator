# Tick 49 — 2026-06-28 22:05 PT — Batch 49 (5/5)

Frontend-focus override active. Five genuinely-new user-facing slices, each
its own commit, gated once at the end, pushed straight to main.

1. **R49.D** `f912bcf` — fpsGraph.formatFpsWindowStats grows opt-in heapMB +
   particles tail; new formatParticleCount (32K/8.5K/800). DebugHUD copy-window
   threads stats.mem + particleCount. Default line byte-unchanged. +23 asserts.
2. **R49.H** `aa59125` — cameraViews.rangePreviewTier (RANGE_PREVIEW_LARGE_AT=10);
   saved-views range chip paints amber + warning tooltip at 10+ rows. +9.
3. **R49.E** `b6ac33e` — store remembers unclamped typed ratio (own key);
   parseRawAspectRatio + formatClampedHint -> zen "(clamped)" tooltip reads
   "typed 12 -> shown 5". +15.
4. **R49.G** `9523f46` — ghostPillSubLabel: caption drag ghost stacks a faint
   wordmark/date sub-line so the drop preview matches the baked pill. +9.
5. **R45.O** `c3d9840` — new debugHud.js: compact/expanded HUD density toggle;
   MIN trims sparkline + trend strips + copy button, keeps the glance rows;
   MIN/FULL header pill, persisted. +18.

Gate: lint 23 err/3 warn == baseline (zero new), build green, 49 test files pass.
+74 asserts. origin/main a9db153..c3d9840. Trigger boilerplate stale again.
