# Tick 15 — 2026-06-21 22:26 PT

**Batch 15 shipped 5/5 — direct to main, gates green, zero lint regression.**

## Slices

| # | Roadmap | One-liner | SHA |
|---|---------|-----------|-----|
| 1 | R15.07  | Spectrum peak-hold fade-out tail — per-bar ring buffer replays the last ~200ms of peaks at falling alpha | `865792f` |
| 2 | R15.13  | Lightbox WASD/hjkl pan auto-repeats while held (60ms beat, 180ms initial delay) | `1a0bf1d` |
| 3 | R15.14  | MIDI user bundles: export/import ALL bundles in a single multi-bundle JSON file | `9d8158b` |
| 4 | R15.16  | Per-attractor MIDI: route CC to ENABLED toggle with Schmitt-trigger hysteresis (0.45..0.55 dead-band) | `e05ccbe` |
| 5 | R15.20  | Named attractor drag-row up/down reorder — propagates through MidiPanel grouping | `bdc10db` |

Plus a small follow-up `6cf966a` to silence a transient `react-hooks/exhaustive-deps` warning caught at the gate (heldKeysRef.current → local capture in the SnapshotGallery cleanup function).

## Substitutions
R15.01 (Echo / trail FBO), R15.02 (3D drag handle), R15.03 (server-rendered OG endpoint), R15.04 (multi-error gutter overlay), R15.05 (custom audio-reactive curve editor) all deferred — first three are dedicated infrastructure batches (render-pipeline refactor / r3f-gizmo integration / backend runtime), R15.04 + R15.05 each need their own focused batch.

Substituted R15.07 / R15.13 / R15.14 / R15.16 / R15.20 from the future queue to keep the batch at 5 great slices instead of padding.

## Gates
- **Lint:** 23 errors / 3 warnings — exactly matches baseline.
  - One transient warning caught at the gate (react-hooks/exhaustive-deps on `heldKeysRef.current` in SnapshotGallery cleanup) — resolved with the local-capture refactor in `6cf966a`.
- **Build:** 912 ms green. Bundle 1.72 MB (gzip 511 KB) — +5 KB from R14.18's 506 KB for the new helpers, cyan multi-bundle buttons, Unicode chevron column, peak-trail Float32Array.
- **Tests:** 39/39 files pass · ~250 fresh asserts this batch.

## What landed where

### Lib (helpers, all unit-tested)
- `waveform.js` — PEAK_TRAIL_LENGTH=12 + ALPHA_MIN/MAX constants, trail Float32Array allocation in `makePeakHoldState`, ring-buffer shift inside `tickPeakHolds`, `readPeakTrail` with current-bar filter + linear alpha lerp, `resetPeakHolds` zeroes trail in place.
- `pinchZoom.js` — `aggregateHeldPan(heldKeysSet, keyClassifier=classifyPanKey)` with diagonal normalisation, cancelling-pair → null, injectable classifier for future remap.
- `midiUserBundleIO.js` — EXPORT_KIND_MULTI envelope + MAX_IMPORT_BYTES_MULTI=64KB cap + `buildExportPayloadMulti` / `serializeUserBundlesMulti` / `makeFilenameMulti` / `parseImportMulti` (bare-array shorthand tolerated) / `downloadUserBundlesFileMulti` / `summarizeImportImpactMulti` (case-insens+trim name match, FIFO drop projection).
- `midiMap.js` — `ATTRACTOR_FIELD_ENABLED` constant + HYSTERESIS_ON/OFF + `applyEnabledHysteresis(wasEnabled, v01)` Schmitt-trigger pure helper; resolveActionForId enabled path reads LIVE attractor + writes only on decision change.
- `namedAttractors.js` — `moveAttractorByIndex` / `moveAttractorUp` / `moveAttractorDown` with ref-equal-on-no-op contract.

### Components
- `WaveformOverlay.jsx` — imports + calls readPeakTrail inside the existing peakHolds branch, paints trail samples oldest-first (newest on top).
- `SnapshotGallery.jsx` — auto-repeat state machine (heldKeysRef Set + repeatTimeoutRef setTimeout + repeatTimerRef setInterval); KeyboardEvent.repeat filtered out; window blur clears state; effect cleanup tears down both timers + the held set; zoom badge title updated to "(hold for smooth sweep)" / "WASD·hold".
- `MidiPanel.jsx` — exportAllUserBundles + importAllUserBundles handlers; PresetBar gets onExportAllUsers + onImportAllUsers props + 2 new cyan buttons; named-attractor MIDI rows render `Enabled` rows with `on/off` label + hysteresis tooltip; section subhead lists all 6 fields.
- `LeftSidebar.jsx` — NamedAttractorsBlock passes onMoveUp/onMoveDown handlers; NamedAttractorRow renders ▲▼ stack between On/Off and ×, boundary-aware disabled state.
- `store.js` — moveNamedAttractorUp/Down actions wired through the helpers, ref-equal short-circuits before save.

## Notes
- Subscribers should not regress on per-attractor MIDI sweeps thanks to the no-op-on-decision-equality guard in the R15.16 setter.
- R15.20's reorder propagates through the MidiPanel grouping (R13.18 + R15.16) automatically because per-attractor MIDI rows are emitted in list order — no extra wiring.
- The R15.07 trail intentionally rides the same `pk` chip toggle as the peak lines; a separate trail toggle felt like clutter for one extra preference.

Push: `f048724..6cf966a main -> main` (verified via fetch).
