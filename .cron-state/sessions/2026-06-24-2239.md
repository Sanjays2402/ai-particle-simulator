# Tick 29 — 2026-06-24 22:39 PT (Cake, cron)

Batch 29 — 5/5 features shipped + pushed to origin/main. All
graduations of Batch 28 slices. All FRONTEND/UX (frontend-focus
override honoured).

Note: the injected task message described a "first tick / 31 unpushed
commits / bootstrap" scenario, but reality contradicted it — main and
origin/main were both at 3367f30 (0 ahead), STATE.md existed, node_modules
present (185 entries), and we were past tick 28. Per the authoritative
prompt ("WORK DIRECTLY ON main ... do NOT create feature branches" +
"Only THIS prompt + STATE.md are authoritative"), worked directly on
main as every prior tick has. No bootstrap / no npm install needed.

## Shipped

1. **R29.43** (commit `b208e9f`) — Persist the hotkey UNDO-chain fade
   curve as a user preference. Graduates R28.43's hard-coded easeInCubic
   with a cycle chip in the MIDI Controller Presets header (linear ->
   Fast-slow -> Slow-fast), persisted to `hotkey-chain-fade-curve-v1`.
   The recursive chain-toast closure reads the live choice through a
   synced ref. New pure helpers in midiPresets.js (isValid / sanitize
   (corrupt -> recommended easeInCubic) / nextCurve / label). Store gains
   hotkeyChainFadeCurve + set/cycle. Chip threaded through PresetBar
   props (lives in that sub-component scope). ~55 fresh asserts.

2. **R29.41** (commit `aa4ed84`) — Bias import preview pip remembers the
   last 3 scope tiers. Prior scopes paint tiny tier-accent dots after
   the pip (newest-prior first, dimming with age) + list in the tooltip
   so users compare "today's low vs last week's high pack". History
   lives at the RightSidebar level (survives preview unmount). New pure
   helpers in biasOverridesIO.js: pushImportScopeHistory (FIFO ring,
   MAX=3, consecutive-dedupe, ref-equal-on-no-op) +
   describeImportScopeHistory. In-memory only (R30.41 will persist).
   ~45 fresh asserts.

3. **R29.42** (commit `1d13929`) — Chain the bulk-unpin undo across
   rapid successive sweeps (parallels R23.35 hotkey chain). Two cleanup
   sweeps within 6s stack into a 2-level undo; each Undo pops the newest
   frame, restores its keys, re-surfaces the toast for the remaining
   depth (x3 -> x2 -> plain). >6s gap resets the chain. New pure helpers
   in presetThumbnails.js: pushBulkUnpinChainFrame (window-gated,
   MAX=8 FIFO), popBulkUnpinChainFrame, formatBulkUnpinChainBadge.
   Carousel holds the stack in a ref. ~50 fresh asserts.

4. **R29.45** (commit `6a1474e`) — Long-press clamp preview chips to
   multi-select + bulk clear (parallels R17.17 attractor multi-select).
   Long-press enters selection mode; taps toggle; Clear N / Cancel bar
   commits a bulk per-cell wipe. Selected = red fill + check; unselected
   = hollow ring. Pure UI state in ClampThresholdPopover; commit loop
   delegates to the already-pinned setClampWarnAttractorFieldOverride
   (no new lib surface). Selection auto-reconciles + auto-exits below 2.
   ~12 fresh asserts pinning the lib commit sequence.

5. **R29.20** (commit `37a57f1`) — Keyboard-accessible gap-drop reorder
   for MIDI binding groups (parallels R15.20 chevron reorder, insert-at-
   gap semantics). Grab handle is focusable: Enter lifts, arrows walk a
   drop cursor through gaps, Enter commits, Escape cancels. Lifted row
   gets an indigo ring; cursor reuses the gap-zone highlight. New pure
   helper stepKeyboardGapCursor in namedAttractors.js (seeds past lifted
   row, skips the 2 no-op adjacent gaps, clamps, null on dead-end).
   Commit reuses dropIndexForGap + moveAttractorByIndex. ~35 fresh
   asserts.

## Gates (run ONCE at end of batch)

- **Lint**: 23 errors / 3 warnings — EXACTLY baseline. Mid-batch the
  count rose to 29 (PresetBar scope mismatch on the R29.43 chip + an
  unused import + a react-hooks/refs flag on the R29.45 long-press
  closure). Fixed all three: threaded the fade-curve via PresetBar
  props, dropped the unused `nextHotkeyChainFadeCurve`/`fadeCurve`,
  wrapped the chip ref read in a named consume helper + a scoped
  eslint-disable with justification. Back to 23/3.
- **Build**: green (~1.0s). 1.86 MB bundle / 546 KB gzip.
- **Unit tests**: ALL 40 test files pass. ~197 fresh asserts this batch.
- **Push**: clean fast-forward 3367f30..37a57f1 -> origin/main; verified
  git fetch + 0 ahead.

## Roadmap state

Batch 29 closed. Batch 30 queued with graduations of every Batch 29
slice: persist the scope-history ring (R30.41); depth pip on the Unpin
footer button itself (R30.42); live fade-preview swatch next to the
curve chip (R30.43); Select-all/none in the chip multi-select bar
(R30.45); aria-live position announcements for the keyboard reorder
(R30.20).
