# 2026-06-19 23:41 PT — Tick 1 (Bootstrap + Batch 1)

Branch: `feature/autoship` (off local main HEAD 169b31b)

## What shipped (5/5 + bootstrap)
1. **b66f8e7** — bootstrap `.cron-state/STATE.md` with 5-feature batch 1 + future queue.
2. **2514538** — `feat(presets): category filter chips` (R1.01) · Space/Physics/Nature/Math/Energy/Shapes pills above the preset list. New side-table mapping (`src/lib/presetCategories.js`) + unit tests.
3. **4699716** — `feat(viz): kaleidoscope / N-fold radial symmetry mode` (R1.02) · 2–12 segment rotational mirror; per-particle cost stays flat (only `leaderCount` preset-fn calls). v2 share URL round-trip + rotation math tests.
4. **c63ab97** — `feat(theme): hue cycle` (R1.05) · 0.5–30 cycles/min global hue drift on top of any theme, live CSS color-bar preview, share URL round-trip, unit test for the cpm→offset math.
5. **b3418a5** — `feat(camera): saved camera views with quick-save shortcut` (R1.04) · Up to 6 named views in localStorage; press `V` to quick-save. `window.__particleCamera` API exposes OrbitControls state. Toast feedback. cameraViews unit tests.
6. **2dc8c73** — `feat(physics): click-to-drop force-field center` (R1.03) · "Place Field on Canvas" arms a single-shot pointer-click handler that raycasts to z=0 and parks the field there. Force math pivots around the new center. Visual halo when armed. Share URL round-trip.

## Gates (single batch run at the end)
- `npm run lint` → 23 problems (20 errors, 3 warnings). All pre-existing on `main` — **no regression**.
- `npm run build` → green in 283ms (vite v8.0.3, 2376 modules).
- Custom test harness: `node src/lib/*.test.mjs` → 4/4 pass (cameraViews, hueCycle, kaleidoscope, presetCategories).

## Self-notes for next tick
- Stop using `git checkout <branch> -- .` to inspect baseline state; it silently overwrites uncommitted work. Use `git stash --include-untracked` instead, or do the comparison on a clean tree.
- Batch was implemented as 6 commits (1 bootstrap + 5 features) — clean, revertible.
- Patch tool occasionally drops the `function Foo() {` line when the new_string starts with a duplicate comment block. Always include 3+ lines of context above the body to avoid this.
- Unit tests use plain `node` (no vitest/jest yet). Pattern: `src/lib/<name>.test.mjs`. Worth standardizing in a future tick.
