# Session 2026-06-26 05:47 PT — Batch 35 (Tick 35)

Frontend-focus override active. Shipped 5/5 — the Batch-35 graduations of
tick 34's brand-new features. All genuinely new user-facing capability,
each extending an EXISTING pure tested module (no new files), wired
straight to the UI. NOT cosmetic filler.

## Context correction
The trigger message claimed "31 unpushed local commits", "first tick",
and "npm install since node_modules was not moved". All stale: git showed
main already 0 ahead / 0 behind origin, node_modules present, .cron-state
bootstrapped (tick 34 shipped earlier the same night). So this was a
normal continuing tick — worked directly on `main` per the authoritative
prompt file, no feature branch, no bootstrap, no install needed.

## Features (one commit each)
| # | Item | Commit | What |
|---|------|--------|------|
| 1 | R35.A | eeeea4e | Debug HUD frame-time (ms) view — press M to flip fps<->ms; same 2s window, faster frame sits higher in both; 16.7/33.3ms budget ref lines; live Frame ms + Avg/Best/Worst + "1% high" + "Over 33ms" rows; FPS|MS header pill. |
| 2 | R35.B | acb2c21 | Composition grid inside the frame — rule-of-thirds (+4 power-point dots) / centre cross / both; backslash cycles; draws with no ratio (full viewport) or snaps to the masked-in frame; LeftSidebar chip row. |
| 3 | R35.C | 8d1a567 | Self-timer burst mode — after the countdown fire 1/3/5 shots 500ms apart; ring shows k/N counter; Escape aborts; dropped-frame catch-up; Layers BurstBtn (shown only when timer armed). |
| 4 | R35.D | 53cfe5e | Perf auto-suggest post-FX remedy — offer to disable the heaviest active pass (DoF>grain>chromatic>vignette, bloom excluded) before dropping particle count; fires on lowest tier too when an FX is on (fxAvailable opt). |
| 5 | R35.E | 4ae463f | Zen ambient auto-orbit — "Zen Auto-Orbit" pref; slow camera drift eases in after ~3.6s stillness in zen; ramps 0->full over ~2.2s; any interaction resets; reduced-motion aware; overrides autoRotate only while active. |

## Pure modules extended (no new files)
- lib/fpsGraph.js: fpsToFrameMs, buildFrameTimeSparklinePoints,
  frameTimeSparklineAttr, frameMsRefLineY, frameMsBandColor,
  summarizeFrameTimeWindow + FRAME_MS_GRAPH_CEIL/FRAME_BUDGET_60/30.
- lib/framingGuides.js: FRAMING_GRIDS + isValidGridId/sanitizeGridId/
  gridLabelForId/nextGridId + computeCompositionGrid (frame-origin aware,
  absolute-pixel verticals/horizontals/power-points).
- lib/selfTimer.js: BURST_COUNTS/BURST_INTERVAL_MS + sanitizeBurstCount/
  labelForBurst/burstShotsDue (fire-the-diff catch-up)/burstComplete.
- lib/perfSuggest.js: POSTFX_EFFECTS + pickHeaviestActivePostFx +
  chooseSuggestionRemedy ({kind:'postfx'|'tier'} union) + fxAvailable opt
  on pushFpsSample (backwards-compat: classic lowest-tier-silent path
  preserved when absent).
- lib/zenMode.js: shouldZenAutoOrbit + zenOrbitSpeed (eased ramp) +
  ZEN_AUTO_ORBIT_SPEED/ZEN_ORBIT_IDLE_MS/ZEN_ORBIT_RAMP_MS.

## UI wiring
- DebugHUD.jsx: ms view + M key (visibleRef avoids nested setState) +
  FPS|MS pill + per-view geometry switch.
- FramingGuides.jsx: grid SVG layer (pointer-events:none) + backslash key;
  refactored to render grid even when no ratio is active.
- LeftSidebar.jsx: Composition Grid chip row + Zen Auto-Orbit ToggleRow.
- ScreenshotTimer.jsx: burst rAF loop + k/N ring counter; TopBar.jsx
  BurstBtn (Layers icon) + burst threaded into the screenshot-timed event.
- PerfAutoSuggest.jsx: remedy router, FX-disable vs tier-drop toast,
  fxAvailable from live store slice.
- ZenMode.jsx: ambient-orbit writer in the cursor-idle rAF loop (wheel +
  keydown count as interaction); ParticleCanvas CameraControls reads
  zenAmbientOrbitSpeed and overrides OrbitControls autoRotate/Speed.

## Store
4 new persisted keys: particle-framing-grid-v1, particle-screenshot-burst-v1,
particle-zen-auto-orbit-v1 (framingGridId, screenshotBurstCount,
zenAutoOrbit). 1 transient: zenAmbientOrbitSpeed (written each frame,
skip-redundant-0 guard, read by the camera).

## Gates (ONE batch gate at end)
- Lint: `npm run lint` → 26 problems (23 errors / 3 warnings) — EXACTLY
  baseline. Every error pre-existing (ParticleCanvas impure/unused set,
  store empty-catch, LeftSidebar R16.18 effect). Proved ParticleCanvas
  stays 9 errors before+after my edit via git-stash diff. All new lib
  code lints 100% clean.
- Build: `npm run build` → green (~737ms, rolldown/vite).
- Tests: `node --test src/lib/*.test.mjs` → 45/45 files pass, 0 fail.
  ~233 fresh asserts (fpsGraph +63, framingGuides +57, selfTimer +37,
  perfSuggest +36, zenMode +40).

## Push
e1d512c..4ae463f -> origin/main, verified fast-forward, 0 ahead / 0 behind.

## Runners
- Tests: `node --test src/lib/*.test.mjs`
- Build: `npm run build` ; Lint: `npm run lint`
- Baseline lint: 23 errors / 3 warnings (all pre-existing). dist/ is
  gitignored — no build artifacts pushed.

## Next tick
Batch 36 queue refilled (R36.A-L, 12 fresh frontend items) + R35.F/G/H
still genuine + the 5 Batch-24 carry items. Plenty unstarted.
