# Batch 51 — 2026-06-29 03:11 PT

Tick 51, frontend-focus override active. Worked directly on `main`,
gated once, pushed `ae0adc6..056dea1`. 5 features / 4 commits.

## Shipped (5/5)
- **R51.B** easing-curve animated dot — `framingGuides.buildEasingPreviewPath`
  emits M..L path of the same bezier samples; LeftSidebar mounts a circle +
  animateMotion on the ACTIVE chip so acceleration is felt over time.
- **R51.M** pinned-crop keyboard reorder — `pinnedReorderTarget(from,count,dir)`
  one-step clamped move; pinned chips get tabIndex + arrow keys + refocus.
  (R51.B+R51.M share framingGuides+LeftSidebar → one commit, documented.)
- **R51.K** apply-all-changed — `calmGatesIO.applyAllChanged` bulk-adopts every
  differing motion; TopBar "apply all changed (N)" above Apply/Cancel at 2+.
- **R51.O** sync both HUD views — `debugHud.syncHudViewModes` + store action;
  Shift+M / shift-click pill snaps fps+ms to one density.
- **R51.H** tier-escalated delete confirm — `cameraViews.rangeDeleteConfirmMessage`
  plain/large/HUGE; CommandPalette deleteSelected uses it.

## Gate (once)
lint 23 err/3 warn == baseline (added warning fixed by dep), build green,
49/49 test files pass. +57 asserts.

## Notes
- Did NOT ship R51.E (zen band re-open) — carried to R52.E. STATE.md fixed.
- Refilled roadmap: Batch 52 (9 fresh frontend items).
- Trigger boilerplate stale: repo in sync, node_modules present.
